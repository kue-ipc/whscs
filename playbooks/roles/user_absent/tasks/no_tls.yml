# no tls
---
- name: Remove cert
  become: true
  ansible.builtin.file:
    path: '{{ webuser.tls_cert }}'
    state: absent

- name: Remove private key
  become: true
  ansible.builtin.file:
    path: '{{ webuser.tls_key }}'
    state: absent
